# 分包加载配置说明

## 📊 分包结构总览

### 主包（73.4KB JS）
```
pages/
├── index/          - 首页/风格报告入口（TabBar）
├── chat/           - 智能问答（TabBar）
├── item-suggestion/ - 单品建议（TabBar）
├── guide/          - 引导页
└── test/           - 测试页
```

### 分包1：报告包（27KB）
```
packageReport/
└── pages/
    └── report/     - 风格报告详情页
```

### 分包2：工具包（8.4KB）
```
packageTools/
└── pages/
    ├── item-result/   - 单品分析结果
    └── item-history/  - 历史记录
```

### 分包3：开发包（26.4KB）
```
packageDev/
└── pages/
    ├── debug/      - 调试页面
    ├── api-test/   - API测试
    └── gpt5-test/  - GPT5测试
```

---

## 🎯 优化效果

### 加载优化
- **主包体积：** 73.4KB（原135KB）→ **减少45.6%**
- **首次加载：** 只需加载主包和TabBar页面
- **按需加载：** 分包在访问时才下载

### 预加载策略
```json
{
  "pages/test/test": {
    "network": "all",
    "packages": ["report"]
  },
  "pages/item-suggestion/item-suggestion": {
    "network": "all",
    "packages": ["tools"]
  }
}
```

- 测试页面完成时，自动预加载报告包
- 单品建议页面打开时，自动预加载工具包
- **用户体验无缝衔接**

---

## 📝 路径映射

### 页面跳转路径已更新

| 原路径 | 新路径 | 说明 |
|--------|--------|------|
| `/pages/report/report` | `/packageReport/pages/report/report` | 报告页 |
| `/pages/item-result/item-result` | `/packageTools/pages/item-result/item-result` | 结果页 |
| `/pages/item-history/item-history` | `/packageTools/pages/item-history/item-history` | 历史页 |
| `/pages/debug/debug` | `/packageDev/pages/debug/debug` | 调试页 |
| `/pages/api-test/api-test` | `/packageDev/pages/api-test/api-test` | API测试 |
| `/pages/gpt5-test/gpt5-test` | `/packageDev/pages/gpt5-test/gpt5-test` | GPT5测试 |

---

## ✅ 测试检查清单

### 1. 主包页面访问测试
- [ ] 首页正常加载
- [ ] TabBar切换正常（首页、聊天、单品建议）
- [ ] 引导页正常显示
- [ ] 测试页正常工作

### 2. 分包页面跳转测试
- [ ] 测试完成后跳转报告页
- [ ] 报告页正常显示内容
- [ ] 单品建议跳转结果页
- [ ] 调试功能正常访问

### 3. 性能验证
- [ ] 首次启动速度明显提升
- [ ] 查看开发者工具控制台，确认分包按需加载
- [ ] 预加载策略生效（测试页→报告包）

---

## 🚀 预期性能提升

| 指标 | 优化前 | 优化后 | 提升 |
|------|--------|--------|------|
| 主包JS大小 | 135KB | 73.4KB | ⬇️ 45.6% |
| 首次加载时间 | ~2-3秒 | ~1-1.5秒 | ⬆️ 40-50% |
| 启动速度 | 慢 | 快 | ⬆️ 显著提升 |

---

## 🔧 开发注意事项

### 添加新页面时：

1. **TabBar页面** → 必须放在主包 `pages/`
2. **首次加载页面** → 放在主包 `pages/`
3. **低频使用页面** → 放在对应分包

### 页面跳转：

```javascript
// ✅ 正确：使用完整路径
tt.navigateTo({
  url: '/packageReport/pages/report/report'
})

// ❌ 错误：使用旧路径
tt.navigateTo({
  url: '/pages/report/report'
})
```

---

## 📌 下一步优化建议

1. ✅ **分包加载** - 已完成
2. ⏳ **图片压缩** - 待执行（运行 `node apply-compressed-images.js`）
3. ⏳ **图片预加载优化** - 待优化
4. ⏳ **懒加载策略** - 可选优化

---

## 💡 使用说明

此配置已经应用到项目中，无需额外操作。

如需回滚：
1. 恢复原 app.json
2. 将分包文件移回 pages/ 目录
3. 更新所有跳转路径



